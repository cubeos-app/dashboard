# CubeOS Dashboard — Production environment
# This file is NOT gitignored and is loaded by Vite during production builds.
# .env (dev overrides) is gitignored and takes precedence locally.

VITE_APP_VERSION=0.2.0-alpha.01
VITE_CUBEOS_DOMAIN=cubeos.cube
